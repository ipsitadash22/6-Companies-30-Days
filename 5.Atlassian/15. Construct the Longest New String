class Solution {
    public int longestString(int x, int y, int z) {
        int minPair = Math.min(x , y);

        if(x == y){
            return 2*(x+y+z);
        }
        return 2*(minPair + minPair + z + 1);
    }
}
